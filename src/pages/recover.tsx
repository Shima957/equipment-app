import ChangePassowrd from '@/components/Setting/ChangePassword';

const Recover = () => {
  return (
    <div className='flex flex-col items-center'>
      <ChangePassowrd />
    </div>
  );
};

export default Recover;
